{
  "project": "workout-tracker",
  "path": "/Users/skhimsara/gymstuff/workout-tracker",
  "captured": "2026-09-04",
  "app_version": "1.8.0 (uncommitted, in-progress)",
  "repo": {
    "branch": "main",
    "remote": "git@github.com:kaavincraig/workout-tracker.git",
    "deploy": "https://kaavincraig.github.io/workout-tracker/",
    "author": "kaavin craig <kaavincraig@gmail.com>",
    "head_commit": "af4568b Fix low-contrast exercise names in light themes"
  },
  "resume_instructions": [
    "Read CONTEXT.md at the repo root — it has the full architecture, invariants, data flow, bug history, dev workflow, and a line-number map of every important function.",
    "The whole app is one file: index.html (single <script> block). Backend: GoogleAppsScript.gs. Reference catalog: exercise_master_list.html. Profile: personal_vitals.json.",
    "Dev: `python3 -m http.server 8765` then open http://localhost:8765/index.html. Local and deployed use different localStorage (different origins)."
  ],
  "status": {
    "committed_and_pushed": [
      "v1.3.0 custom workout builder + custom_workouts.json + auto-push (push buttons removed) + resolveDayId underscore fix",
      "v1.3.1 pull no longer jumps to Day 1",
      "v1.4.0 custom_workouts.json export button + Auto-Save Ready badge moved to footer",
      "v1.5.0 PR tab (🏆) + live PR badges + rest timer (⏱) + exercise history modal + Settings tab (🎛️)",
      "v1.5.1 (98d4d75) delete custom workout verifies on Drive: 4-state toast + console log of raw array",
      "v1.5.2 (0079349) Pull Custom mirrors deletions across devices (drop local customs absent from drive) + failed-read guard",
       "v1.6.0 (ddbd221) #6 Session Stats: new 📊 tab (between + and 🏆) with all-time summary (workouts / total sets / total + avg volume lbs) + per-session rows (newest first: sets, volume, best est 1RM via Epley weight×(1+reps/30) naming the exercise+set). Bodyweight/zero-rep rows excluded. Engine: estOneRM / computeSessionStats / renderStatsTab (after renderPRsTab). Rest-timer presets trimmed to 30s/60s/90s. App version bumped to 1.6.0.",
       "v1.7.0 (83e2a03) #1 Progress Charts: new 📈 tab (between + and 📊) with per-exercise line charts over time (pure inline SVG, no deps). Metric toggle Peak set (heaviest loaded set) / Volume (lbs). Only exercises with a loaded set in 2+ sessions are charted (BW-only/single-session excluded). Engine: collectExerciseNames / getExerciseProgress / buildLineSVG / renderProgressTab / switchProgressMetric (after renderStatsTab). App version bumped to 1.7.0.",
       "v1.8.0 (1f7f531 + af4568b) #8 Theme picker: ⚙️ Settings gains 🎨 picker originally with 6 dark themes (Midnight Slate default, Steel Cobalt, Graphite Mono, Aurora Teal, + 2 light: Glacier and Ivory Chalk). THEMES array + [data-theme=\"…\"] CSS blocks + appTheme in localStorage. Light themes had a scoped CSS override block to force readable text on themed surfaces, and the exercise name / dimmed state forced with !important (af4568b). Bump window.APP_VERSION -> 1.8.0. Engine: THEMES/getStoredTheme/isKnownTheme/applyTheme/renderThemePicker (after toggleRestTimerEnabled); picker CSS .theme-option/.theme-swatch/.theme-check; selector #themePicker."
    ],
    "uncommitted": [
      "Removed the two LIGHT themes (Glacier + Ivory Chalk) from v1.8.0, leaving 4 dark themes (default / steel-cobalt / graphite-mono / aurora-teal). Changes: deleted the glacier + ivory-chalk [data-theme] CSS blocks, the scoped light-theme override block (the big selector list + .exhist-chip:not(.pr) accent rule + .exercise-name / .is-dimmed force), and the 2 THEMES array entries. Verified: node --check passes; headless jsdom — THEMES.length===4, isKnownTheme('glacier')/('ivory-chalk') false, picker shows 4 options, a stale localStorage appTheme='glacier' falls back to default (attr removed, stored value reset). CONTEXT.md + context-resume.json updated to reflect the removal."
    ],
    "open_bug": null,
    "resolved_bugs": [
      {
        "id": "pull-doesn-drop-deleted-customs",
        "fixed": "v1.5.2",
        "report": "Delete custom on device A; device B (which had pulled it earlier) still shows it after re-pulling from Drive.",
        "how": "Root cause was handleCustomWorkoutsResponse doing a union/merge (added drive customs, never removed absent ones). Changed to a MIRROR: before merging, any local custom id not in the drive array is deleted from workout_split_plan + customworkout_<id> + customworkout_indices. Guard: if the read returns an object ({status:…} or {workout_logs:…}) instead of an array, bail early with a warning so a failed/unauthorized pull can't wipe local customs.",
        "test": "pull_mirror_test.mjs in opencode scratch dir (all pass: empty-drive drops stale local; union merges updates; unauthorized + wrong-file preserve local)."
      },
      {
        "id": "cloud-sync-delete-not-propagating",
        "fixed": "v1.5.1",
        "how": "deleteCustomWorkout now does a silent push + a 2s-delayed verifyDriveFile('custom_workouts') GET that toasts one of 4 states (✅ removed / ❌ unauthorized → ?key=MYJEY / ❌ old-script-ignores-target / ⚠️ still-on-Drive) and logs the raw Drive array to console. This surfaces the previously-silent fire-and-forget failure modes.",
        "note": "Root cause was a silent push (no feedback) — the code path was already correct as long as the saved /exec URL carried ?key=MYJEY. The verify makes that requirement visible instead of a silent no-op. Test: delete_verify_test.mjs in opencode scratch dir (ok / unauthorized / still-there all pass)."
      }
    ]
  },
  "key_mechanics": {
    "single_active_day_lock": "active_timer_<dayId> in localStorage gates editing; getActiveRunningDay() walks split plan.",
    "day_id_parsing": "resolveDayId(id) matches real split_plan keys (handles custom ids with underscores). Do NOT reintroduce /(day[0-9]+)/.",
    "custom_flag": "day.custom === true distinguishes custom plans (rendered with a delete button) from static Day 1-4.",
    "hydration": "today's log -> white (user-edited); non-today log -> gray via setGrayValue().",
    "pins": { "delete_one_log": "1981", "clear_all_history": "1986" },
    "autosave": "triggerAutoSave -> 600ms debounce -> saveSession; only fires when workout active; local save independent from cloud.",
    "session_stats": "estOneRM Epley: weight×(1+reps/30). computeSessionStats(log) sums loaded sets' weight×reps into volume; best 1RM = max over loaded sets. BW (weight 0) / zero-rep rows excluded. renderStatsTab renders #statsContainer (summary grid + .ss-row rows newest first).",
    "progress_charts": "renderProgressTab renders #progressContainer. One SVG line per (normalized) exercise across sessions (>=2 loaded pts). getExerciseProgress -> points {date,weight,reps,volume}; buildLineSVG draws polyline + dots + peak ★; switchProgressMetric toggles peak vs volume. Single-session/BW-only exercises are hidden.",
    "themes": "4 themes (default/Midnight Slate, steel-cobalt, graphite-mono, aurora-teal) — all DARK. (Glacier + Ivory Chalk were added in 1f7f531, then removed.) CSS vars via [data-theme=\"id\"] blocks that override :root (same --* names: --bg-color/--card-bg/--card-header/--text-main/--text-muted/--accent/--accent-glow/--border/--success/--input-bg/--btn-stepper + --accent-rgb + --on-accent). THEMES array (index.html, right after toggleRestTimerEnabled) drives the Settings picker; selection under appTheme in localStorage, applied on load (applyTheme+renderThemePicker after setRestTimerEnabled). accent tints use rgba(var(--accent-rgb), x); accent-button text uses var(--on-accent). Adding a theme = new [data-theme] block + THEMES entry; for a LIGHT theme ALSO add a scoped override block (before `* {`) switching themed-surface text to var(--text-main), forcing .exercise-name (+.is-dimmed) with !important, and ensuring inline color:#fff surfaces use var(--text-main). Default id 'default' removes the data-theme attribute; an unknown/stored id falls back to default."
  },
  "gotchas_learnt": [
    "Apps Script changes require REDeploy (Manage deployments -> New version) to the same /exec URL; save alone is not enough.",
    "Hidden-iframe form POST is fire-and-forget: client cannot see success/failure. 'Verify On Drive' does a follow-up GET to detect problems.",
    "302 redirect (to Google sign-in) with a large HTML body in DevTools = the /exec URL is hitting an auth wall (wrong key value in URL, or deployment access/scope not granted to Anyone). Both pull and push silently fail in that state while local save still works.",
    "jsdom is installed at /var/folders/t_/dkx509097s3dbl3xhpfmdq980000gn/T/opencode (node_modules) — use it for headless DOM tests; force process.exit() since the app's setInterval keeps the loop alive."
  ],
  "next_actions_for_next_session": [
    "1. (DONE) CONTEXT.md + context-resume.json are tracked in the repo; keep updating both after each change.",
    "2. (DONE v1.5.1) cloud-sync-delete-not-propagating is fixed: delete now verifies on Drive with a 4-state toast + console log of the raw array. Re-test in the browser against a correctly-deployed Apps Script if the user still reports the deletion not landing.",
    "3. (DONE v1.6.0) #6 Session Stats shipped. (DONE v1.7.0) #1 Progress Charts shipped. Both Wave 1 + #6 + #1 complete.",
    "4. Candidate next feature (PLAN): #7 Progression suggestion (last-two-logs diff per exercise) or #5 Body weight + chart (next easiest in Wave 2).",
    "5. (IN-PROGRESS v1.8.0) Theme picker in Settings. Glacier + Ivory Chalk (the 2 light themes) were just REMOVED, leaving 4 dark themes (default/steel-cobalt/graphite-mono/aurora-teal). UNCOMMITTED — commit + push when done. To add a theme: [data-theme=…] CSS block + one THEMES entry; a light theme also needs a scoped override block (before `* {`). Palette samples: theme_options.html / theme_samples.html / theme_more.html (repo root, untracked, safe to delete).",
    "6. After any index.html change, run the node --check snippet from CONTEXT.md before committing.",
    "7. Bump window.APP_VERSION (index.html line 10) per change; it's displayed in the footer."
  ]
}
